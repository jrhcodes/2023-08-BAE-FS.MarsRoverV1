package com.jrhcodes;

public class Rover {/*
    private final Pose pose;
    List<Pose> path;

    public Rover(Pose pose) {
        this.pose = pose;
        this.path = new ArrayList<>;
        this.path.add(pose);
    }

    public void moveForward() {
        pose.addVector(direction.getDx(), direction.getDy());
    }

    public static void main(String[] args) {
        Rover rover = new Rover( new Pose( new Point(0, 0), Direction.NORTH));
        rover.moveForward(); // Moves the rover one step forward (North)
        System.out.println("Current position: " + rover.getPosition());
    }
*/}
